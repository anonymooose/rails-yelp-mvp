<h1>Restaurants</h1>

<div class="container">
    <% @restaurants.each do |restaurant| %>
      <div class="row">
            <%= link_to restaurant.name, restaurant_path(restaurant) %>
      </div>
    <% end %>
  <%= link_to "Create Restaurant", new_restaurant_path, class: 'btn btn-success' %>
</div>

